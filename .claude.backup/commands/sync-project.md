---
description: Init or update agent config in current project
---

If agent config exists, back it up and update it. If not, initialize Claude Code, Codex, and shared skills.

```bash
agent-sync sync --project .
```
