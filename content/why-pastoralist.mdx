---
title: "Why Pastoralist: Dependency Overrides Need an Audit Trail"
date: "2026-02-20"
path: "/why-pastoralist"
meta: "Why I built Pastoralist, a tool that tracks, scans, and cleans npm, yarn, pnpm, and bun dependency overrides. I audited 10 major OSS projects, found 135 overrides with no inline rationale, tested cleanup against five larger override sets, and then reran the study across fork branches synced on May 25, 2026."
categories:
  - code
  - security
  - dependencies
tags: ["Open Source", "npm", "Security", "Dependencies", "Pastoralist"]
---

import { PastoralistStudyCharts } from "../lib/components/content/why-pastoralist";

Every Node.js codebase eventually accumulates `overrides` or `resolutions` in `package.json`.

A CVE lands in a transitive dependency. Someone pins a safer version. The PR gets merged. Six months later, the override is still there, but the reason is gone.

---

## TL;DR

- I audited 10 popular OSS projects and found 135 dependency overrides or resolutions across their root `package.json` files.
- None of those override entries included an inline rationale explaining _why_ the override existed.
- In a cleanup sample of five projects, 140 of 171 overrides were unused. That's 82% dead weight.
- In a May 25, 2026 follow-up against fork branches synced from upstream, Pastoralist generated 275 appendix entries across 5,252 scanned package manifests in 12 PRs.
- The median Pastoralist scan in the timing sample took 252ms with OSV security checks enabled.
- [Pastoralist](https://yowainwright.github.io/pastoralist/) tracks, scans, and cleans your overrides automatically.

---

## The Problem

Here's a `package.json` override block I bet looks familiar:

```json
// [title: package.json - undocumented overrides]
{
  "overrides": {
    "semver": "^7.5.4",
    "ws": ">=8.17.1",
    "axios": "1.12.0",
    "lodash": ">=4.17.23"
  }
}
```

Now try answering these questions:

1. _Why_ is each override here?
2. Which packages in your dependency tree actually depend on it?
3. Is it still needed?
4. Did a new CVE show up since you pinned it?

Most teams cannot answer those questions from `package.json` alone. That's the problem.

---

## The Data: 10 Popular OSS Projects

I wanted to know how widespread this is, so I looked at the root `package.json` of 10 well-known open source projects and counted every `overrides`, `resolutions`, and `pnpm.overrides` entry I found.

These numbers are a February 20, 2026 snapshot captured with Pastoralist 1.11.0, not a leaderboard. Stars, dependency trees, and override counts move. The useful signal is the pattern: projects add overrides, but the reason rarely lives with the override.

<p className="post__table-title">Initial Override Snapshot</p>

| Project          | Stars | Field                       | Overrides |                  Likely Security-Related |
| ---------------- | ----: | --------------------------- | --------: | ---------------------------------------: |
| **n8n**          |  ~51k | `pnpm.overrides`            |        58 |                                      ~17 |
| **Storybook**    |  ~85k | `resolutions`               |        19 |                                       ~3 |
| **Prisma**       |  ~41k | `pnpm.overrides`            |         5 | 5 (form-data, hono, jws, tar-fs, lodash) |
| **VS Code**      | ~167k | `overrides`                 |         2 |                       1 (node-gyp-build) |
| **React**        | ~233k | `resolutions`               |         2 |                                1 (jsdom) |
| **React Native** | ~120k | `resolutions`               |         3 |              2 (on-headers, compression) |
| **Supabase**     |  ~77k | `pnpm.overrides`            |         0 |       0 (cleaned up since initial audit) |
| **Angular CLI**  |  ~27k | `overrides` + `resolutions` |         3 |                           1 (typescript) |
| **Gatsby**       |  ~55k | `resolutions`               |         2 |                                        0 |
| **Cal.com**      |  ~34k | `resolutions`               |        41 |                                       3+ |

**That's 135 overrides across 10 projects. At least 33 looked security-related, or about 24%. None of the override entries documented why they existed.**

---

Some observations:

- **n8n** has 58 overrides. Some look like security patches (`ws`, `semver`, `axios`). Some look like compatibility pins (`date-fns`, `chokidar`, `esbuild`). Some are forks (`expr-eval@2.0.2` remapped to `npm:expr-eval-fork@3.0.0`). The intent is understandable only if you already know the history.
- **Prisma** is interesting because _all 5_ of their overrides are security-related. They use `>=` version ranges which suggests "at least this version to patch a CVE." But which CVE? You'd have to go digging.
- **Supabase** had 4 overrides when this was first written and has since removed all of them. That's the right outcome, but it only happens when stale overrides are visible.
- Even **React**, with just 2 resolutions, doesn't explain why `jsdom` is pinned to `22.1.0`. Is it a security fix? A compatibility issue? A test environment workaround?

The pattern is consistent: overrides go _in_, context stays _out_.

### What's actually still needed?

Counting overrides tells you how much hidden state exists. Running Pastoralist tells you how much of it still matters.

I ran Pastoralist against five larger override sets to see which overrides were still active in the current dependency tree:

<p className="post__table-title">Unused Override Cleanup Sample</p>

| Project         | Overrides | Unused | Unused % |
| --------------- | --------: | -----: | -------: |
| n8n             |        58 |     56 |      97% |
| TanStack Router |        51 |     41 |      80% |
| Cal.com         |        41 |     35 |      85% |
| Next.js         |        16 |      3 |      19% |
| Prisma          |         5 |      5 |     100% |

**140 of 171 tested overrides were no longer doing anything. That's 82%.**

Prisma is the clearest case: all 5 overrides were security patches for packages that had since left the dependency tree. The patches were not harmful, but they were carrying old operational context forever.

---

## Follow-Up: Current Branch Findings

On May 25, 2026, I reran the study against clean fork branches synced from upstream for the projects that still had meaningful override or resolution surfaces. This skipped the old local work branches and opened PRs against my forks rather than the upstream projects.

Pastoralist added an audit trail for **275 override or resolution entries** across **5,252 scanned package manifests**:

The win is reviewable in the PR diffs: one Pastoralist run turns a plain override block into a package-by-package ledger that records which dependency still needs the override and why the constraint exists.

<PastoralistStudyCharts />

<p className="post__table-title">Fork PR Study Results</p>

| Project     | Fork PR                                                                          | Upstream ref                      | Entries | Package manifests scanned | Appendix entries |
| ----------- | -------------------------------------------------------------------------------- | --------------------------------- | ------: | ------------------------: | ---------------: |
| n8n         | [yowainwright/n8n#3](https://github.com/yowainwright/n8n/pull/3)                 | `n8n-io/n8n@master`               |      79 |                       604 |               79 |
| Cal.com     | [yowainwright/cal.com#1](https://github.com/yowainwright/cal.com/pull/1)         | `calcom/cal.diy@main`             |      89 |                       375 |               89 |
| Grafana     | [yowainwright/grafana#1](https://github.com/yowainwright/grafana/pull/1)         | `grafana/grafana@main`            |      26 |                       259 |               26 |
| Storybook   | [yowainwright/storybook#1](https://github.com/yowainwright/storybook/pull/1)     | `storybookjs/storybook@next`      |      18 |                       433 |               18 |
| Next.js     | [yowainwright/next.js#1](https://github.com/yowainwright/next.js/pull/1)         | `vercel/next.js@canary`           |      16 |                       340 |               16 |
| Material UI | [yowainwright/material-ui#1](https://github.com/yowainwright/material-ui/pull/1) | `mui/material-ui@master`          |      13 |                       161 |               13 |
| Prisma      | [yowainwright/prisma#1](https://github.com/yowainwright/prisma/pull/1)           | `prisma/prisma@main`              |      10 |                       211 |               10 |
| Nextra      | [yowainwright/nextra#1](https://github.com/yowainwright/nextra/pull/1)           | `shuding/nextra@main`             |       5 |                       102 |                5 |
| Nimbalyst   | [yowainwright/nimbalyst#1](https://github.com/yowainwright/nimbalyst/pull/1)     | `nimbalyst/nimbalyst@main`        |       4 |                     2,144 |                4 |
| es-check    | [yowainwright/es-check#413](https://github.com/yowainwright/es-check/pull/413)   | `dollarshaveclub/es-check@master` |       1 |                       446 |                1 |

Two smaller TanStack projects also made good supporting studies:

<p className="post__table-title">TanStack Supporting Studies</p>

| Project           | Fork PR                                                                    | Upstream ref             | Entries | Package manifests scanned | Appendix entries |
| ----------------- | -------------------------------------------------------------------------- | ------------------------ | ------: | ------------------------: | ---------------: |
| TanStack Store    | [yowainwright/store#1](https://github.com/yowainwright/store/pull/1)       | `TanStack/store@main`    |       7 |                        61 |                7 |
| TanStack Devtools | [yowainwright/devtools#1](https://github.com/yowainwright/devtools/pull/1) | `TanStack/devtools@main` |       7 |                       116 |                7 |

The practical finding changed from "projects have undocumented overrides" to something stronger: Pastoralist can generate the missing operational record across large, real dependency graphs. n8n and Cal.com are the largest examples, but the same pattern holds for framework, database, design-system, docs, and developer-tooling repos.

I excluded a few recognizable names from this follow-up table. TanStack Router no longer had root overrides or resolutions on the current upstream branch. React Router and TanStack Hotkeys need separate validation because their workspace-only override shape triggered removal behavior instead of producing appendix entries.

---

## What Pastoralist Does

[Pastoralist](https://github.com/yowainwright/pastoralist) solves this by doing three things:

### 1. Track

When you run `pastoralist`, it scans your overrides and builds an **appendix** that documents each one:

```json
// [title: package.json - Pastoralist appendix]
{
  "pastoralist": {
    "appendix": {
      "lodash@>=4.17.23": {
        "dependents": {
          "express": "^4.18.0",
          "async": "^3.2.0"
        },
        "ledger": {
          "reason": "Security vulnerability CVE-2021-23337",
          "severity": "high",
          "addedDate": "2025-01-15T00:00:00.000Z"
        }
      }
    }
  }
}
```

Now you know _why_ it's there, _what_ depends on it, and _when_ it was added. That's the context nobody writes down manually.

### 2. Scan

Pastoralist checks your overrides against vulnerability databases. It ships with support for 4 providers:

- **OSV** (free, no auth required)
- **GitHub Advisory Database**
- **Snyk**
- **Socket**

If a new CVE affects one of your overridden packages, Pastoralist catches it and can auto-generate a fix. Interactive mode lets you choose which fixes to apply.

### 3. Clean

Overrides that are no longer needed get flagged for removal. If the parent dependency updated and no longer pulls in the vulnerable version, Pastoralist tells you the override is stale and can be removed after your normal CI checks pass.

This is the part people forget about most. I've seen projects with overrides for packages that haven't been in their dependency tree for years.

---

## Before and After

**Before Pastoralist:** Mystery overrides with no context.

```json
// [title: package.json - before Pastoralist]
{
  "overrides": {
    "trim": "^0.0.3"
  }
}
```

**After Pastoralist:** Full audit trail.

```json
// [title: package.json - after Pastoralist]
{
  "overrides": {
    "trim": "^0.0.3"
  },
  "pastoralist": {
    "appendix": {
      "trim@^0.0.3": {
        "dependents": {
          "remark-parse": "4.0.0"
        },
        "ledger": {
          "addedDate": "2025-01-15T00:00:00.000Z",
          "reason": "Security vulnerability",
          "severity": "high",
          "cve": "CVE-2020-7753"
        }
      }
    }
  }
}
```

---

## CI/CD

Pastoralist ships as a GitHub Action with three modes:

- **`check`** -- Validates overrides are documented and current. Fails on vulnerabilities when `fail-on-security` is enabled.
- **`update`** -- Updates overrides and the appendix in place.
- **`pr`** -- Creates a pull request with the changes.

A weekly cron to keep overrides clean:

```yml
# [title: .github/workflows/pastoralist.yml]
name: pastoralist
on:
  schedule:
    - cron: "0 5 * * 1"
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: yowainwright/pastoralist@v1
        with:
          mode: pr
          check-security: true
          fail-on-security: true
```

For CI pipelines, `--quiet --checkSecurity` exits with code 1 if vulnerabilities are found. JSON output is available for programmatic consumption.

---

## Monorepo Support

If your `package.json` has a `workspaces` field, Pastoralist auto-detects it. Set `depPaths: "workspace"` and it scans all workspace packages. The appendix stays in the root so workspace packages stay clean.

---

## Getting Started

```bash
# [title: Install Pastoralist]
npm i pastoralist -D
```

Then run it:

```bash
# [title: Run Pastoralist]
npx pastoralist
```

Or add it to `postinstall`:

```json
// [title: package.json - postinstall]
{
  "scripts": {
    "postinstall": "pastoralist"
  }
}
```

Works with npm, yarn, pnpm, and bun. The [docs site](https://yowainwright.github.io/pastoralist/) has interactive demos and StackBlitz sandboxes if you want to try it without installing anything.

---

## How fast is it?

I timed Pastoralist against seven projects, including the cleanup sample above, with a full OSV security scan (`--checkSecurity --securityProvider osv`). No API key, no auth:

<p className="post__table-title">Pastoralist Scan Timing Sample</p>

| Project         | Overrides |    Time |
| --------------- | --------: | ------: |
| n8n             |        58 |   416ms |
| TanStack Router |        51 |   252ms |
| Cal.com         |        41 |   406ms |
| Storybook       |        19 |   177ms |
| Next.js         |        16 | 1,050ms |
| Prisma          |         5 |   196ms |
| React Router    |         0 |   108ms |

The median run was 252ms. The three largest override sets all finished under 500ms, network call included. Next.js took 1,050ms because it also evaluates patch files alongside its overrides. React Router had no overrides and finished in 108ms.

The practical takeaway: this is fast enough to run in CI or on a weekly scheduled PR without adding meaningful friction.

---

## Why I Built This

I wrote a post a few years back about [building your own Dependabot](/build-your-own-dependabot) and another about [Snyk and Dependabot ignore configs](/snyk-dependabot-ignore-configs). Both tools are great for _dependencies_. But neither one manages _overrides_. Overrides sit in a blind spot between dependency management and security scanning. Pastoralist fills that gap.

If you're maintaining a project with overrides, give it a shot. If your project does not have overrides yet, run it once for a clean baseline so the first emergency pin does not become mystery state.

Happy Hacking!
